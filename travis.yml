
# Enable C++ support
language: cpp
# Compiler selection
compiler:
  - clang
  - gcc
# Build steps
script:
  - git submodule init
  - git submodule update
  - mkdir build
  - cd build
  - cmake .. && make

